# Николай Мишковец #
## email: frededdd@mail.ru ##
## Опыта работы нет, есть огроное желание учиться, узнавать новое, изучать программирование. ##
## Изучаю самостоятельно HTML, CSS, JavaScript. ##
## Пример программа проверяющая таблицу умножения): ##
~~~ let zikl = true;
let sprot = 0;
let snot = 0;

alert("Если надоест, то в результате вычислений поставь 0 !");

while (zikl) {
  let num1 = Math.floor(Math.random() * 10 + 1);
  let num2 = Math.floor(Math.random() * 10 + 1);
  let rez = num1 * num2;
  let polz = prompt("Введите результат вычисления " + num1 + " * " + num2);

  if (rez == polz) {
    alert("Молодец, правильно! " + num1 + " * " + num2 + " = " + rez);
    sprot = sprot + 1;
  } else {
    if (polz == 0) {
      zikl = false;
    } else {
      alert("Не правильно, иди учи таблицу умножения! " + num1 + " * " + num2 + " = " + rez);
      snot = snot + 1;
    }
  }
}

alert("Подведем итоги. Количество правильных ответов составляет = " + sprot + " Количество не правильных ответов составляет = " + snot + " Таблицу умножения ты знаешь на " + (sprot / (sprot + snot)) * 100 + " %");
~~~
## Опыта нет, только изучаю. ##
## Курсы никакие не проходил, только самостоятельное изучение. ##
## Английский начальный. ##
